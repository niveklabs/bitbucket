# bitbucket Provider

[embedmd]:# (bitbucket.tf)
```tf
provider "bitbucket" {
  version = "1.1.0"

  # password - (required) is a type of string
  password = null
  # username - (required) is a type of string
  username = null
}
```
